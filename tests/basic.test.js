describe('simple test to play with github actions', () => {
    it('The strings should equal', () => {
        const first = 'Hello World!'
        const second = 'Hello World!'
        expect(first).toBe(second)
    })
})